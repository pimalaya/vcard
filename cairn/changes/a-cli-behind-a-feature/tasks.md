---
cairn: tasks
change: a-cli-behind-a-feature
---

- [ ] Settle the three open questions in the proposal
- [ ] Add the `cli` feature and the binary target, off by default
- [ ] `parse` and `validate` with their `*Output` types and `--json`
- [ ] `convert` over the jCard and JSContact codecs, both directions
- [ ] `build` over `VcardBuilder`
- [ ] `merge` over `VcardMerge`, if it survives the first question
- [ ] README section and `--help` text, per the repo layout conventions
